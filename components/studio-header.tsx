import {Dna,Layers} from 'lucide-react';
export function StudioHeader({single=false}:{single?:boolean}){return <header className="topbar studio-topbar"><a className="brand studio-brand" href="/" aria-label="棱序 Primer Studio 首页"><span className="studio-mark"><Dna size={23}/></span><span>棱序<small>PRIMER STUDIO</small></span></a><nav className="edition-nav" aria-label="工作台版本"><a href="/" className={!single&&true?'edition-current':undefined} aria-current={!single&&true?'page':undefined}><Layers size={15}/>高通量<span>50</span></a><a href="https://human-cds-primer-batch-lab.lwhjq6666.chatgpt.site/" className={!single&&false?'edition-current':undefined} aria-current={!single&&false?'page':undefined}>批量版<span>10</span></a><a href="/single" className={single?'edition-current':undefined} aria-current={single?'page':undefined}>单基因<span>1</span></a></nav></header>}
export function StudioHeading({single=false}:{single?:boolean}){return <div className="page-heading studio-heading"><div><span className="eyebrow">HUMAN CDS · PRECISION DESIGN</span><h1>棱序<span>{single?'单基因引物设计':'引物设计工作台'}</span></h1><p>{single?'选择参考转录本，设计完整 CDS 扩增引物。':'从目标基因到候选引物，一站完成设计与验证。'}</p></div><div className="studio-capacity"><div><strong>{single?'1':'50'}</strong><span>{single?'目标 / 次':'目标 / 批'}</span></div><div><strong>{single?'18–25':'1 + 4'}</strong><span>{single?'引物长度 / nt':'优先候选与备选 / 目标'}</span></div></div></div>}
